name: Nightly QA Deployment

on:
  schedule:
    - cron: '0 2 * * *'  # Runs at 2 AM UTC daily
  workflow_dispatch:     # Also allows manual triggering

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: fsv-app
  QA_EC2_INSTANCE_ID: i-0f16adc52da0f3447  # Permanent QA EC2 instance
  DB_HOST: fixedstrike.cdgrdbxezfp6.us-east-1.rds.amazonaws.com
  DB_NAME: fixedstrike
  DB_USER: admin
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout infrastructure repo
        uses: actions/checkout@v3
        
      - name: Checkout source repo
        uses: actions/checkout@v3
        with:
          repository: p-archamb/FixedStrikeVolatility
          path: source
          token: ${{ secrets.SOURCE_REPO_PAT }}
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Build application image locally
        id: build-image
        run: |
          cd source
          IMAGE_TAG=$(date +%Y%m%d%H%M%S)
          
          docker build -t fsv-app:$IMAGE_TAG .
          
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
      
      - name: Launch verification EC2 instance
        id: launch-ec2
        run: |
          # Get default VPC ID
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=isDefault,Values=true" --query "Vpcs[0].VpcId" --output text)
          echo "Using default VPC: $VPC_ID"
          
          # Get first subnet in default VPC
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query "Subnets[0].SubnetId" --output text)
          echo "Using subnet: $SUBNET_ID"
          
          # Get or create security group
          SG_NAME="verification-instance-sg"
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=$SG_NAME" "Name=vpc-id,Values=$VPC_ID" --query "SecurityGroups[0].GroupId" --output text)
          
          if [ "$SG_ID" = "None" ] || [ -z "$SG_ID" ]; then
            echo "Creating new security group: $SG_NAME"
            SG_ID=$(aws ec2 create-security-group --group-name $SG_NAME --description "Security group for verification instances" --vpc-id $VPC_ID --query "GroupId" --output text)
            
            # Allow SSH, HTTP, and HTTPS
            aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 22 --cidr 0.0.0.0/0
            aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 80 --cidr 0.0.0.0/0
            aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 443 --cidr 0.0.0.0/0
            aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 3000 --cidr 0.0.0.0/0
          fi
          
          echo "Using security group: $SG_ID"
          
          # Get the latest Amazon Linux 2 AMI
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters "Name=name,Values=amzn2-ami-hvm-2.0.*-x86_64-gp2" "Name=state,Values=available" \
            --query "sort_by(Images, &CreationDate)[-1].ImageId" \
            --output text)
          
          echo "Using AMI: $AMI_ID"
          
          # Create user-data script to set up Docker
          cat > user-data.sh << 'EOF'
          #!/bin/bash
          echo "Instance started at $(date)" > /tmp/startup.log
          yum update -y
          amazon-linux-extras install -y docker
          systemctl start docker
          systemctl enable docker
          
          # Create directories for the app
          mkdir -p /opt/app
          EOF
          
          # Launch instance for verification
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id $AMI_ID \
            --instance-type t2.micro \
            --security-group-ids $SG_ID \
            --subnet-id $SUBNET_ID \
            --user-data file://user-data.sh \
            --tag-specifications 'ResourceType=instance,Tags=[{Key=Name,Value=App-Verification-Instance}]' \
            --query 'Instances[0].InstanceId' \
            --output text)
            
          echo "instance_id=$INSTANCE_ID" >> $GITHUB_OUTPUT
          
          # Wait for instance to be running
          aws ec2 wait instance-running --instance-ids $INSTANCE_ID
          
          # Get public IP
          PUBLIC_IP=$(aws ec2 describe-instances \
            --instance-ids $INSTANCE_ID \
            --query 'Reservations[0].Instances[0].PublicIpAddress' \
            --output text)
          echo "public_ip=$PUBLIC_IP" >> $GITHUB_OUTPUT
          
          # Wait for instance to be ready for SSH
          echo "Waiting for instance to be ready for SSH..."
          while ! ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 -i ~/.ssh/id_rsa ec2-user@$PUBLIC_IP echo "SSH connection successful" 2>/dev/null; do
            echo "Waiting for SSH connection..."
            sleep 10
          done
      
      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan ${{ steps.launch-ec2.outputs.public_ip }} >> ~/.ssh/known_hosts
      
      # Step 4: Save Docker image as tar, copy to EC2, and run
      - name: Deploy application to verification EC2
        run: |
          # Save the Docker image to a tar file
          IMAGE_TAG=${{ steps.build-image.outputs.image_tag }}
          docker save fsv-app:$IMAGE_TAG -o app-image.tar
          
          # Copy the image to the EC2 instance
          PUBLIC_IP=${{ steps.launch-ec2.outputs.public_ip }}
          scp -i ~/.ssh/id_rsa app-image.tar ec2-user@$PUBLIC_IP:/tmp/
          
          # Load and run the Docker image on the EC2 instance
          ssh -i ~/.ssh/id_rsa ec2-user@$PUBLIC_IP << EOF
            # Load the Docker image
            sudo docker load -i /tmp/app-image.tar
            
            # Run the application container
            sudo docker run -d -p 80:80 -p 3000:3000 \
              -e DB_HOST="${{ env.DB_HOST }}" \
              -e DB_NAME="${{ env.DB_NAME }}" \
              -e DB_USER="${{ env.DB_USER }}" \
              -e DB_PASSWORD="${{ env.DB_PASSWORD }}" \
              fsv-app:$IMAGE_TAG
            
            # Remove the image tar to save space
            rm /tmp/app-image.tar
          EOF
          
          echo "Application deployed to verification EC2 instance"

      - name: Wait for application to start
        run: |
          echo "Waiting for application to initialize..."
          sleep 60  # Initial wait
          
          # Get the public IP
          PUBLIC_IP=${{ steps.launch-ec2.outputs.public_ip }}
          
          # Check if the application is responding to HTTP requests
          echo "Checking if the application is responding..."
          
          # Try HTTP request with retries
          MAX_RETRIES=10
          RETRY_COUNT=0
          HTTP_SUCCESS=false
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$HTTP_SUCCESS" = false ]; do
            if curl -s -m 10 http://$PUBLIC_IP/ > /dev/null; then
              HTTP_SUCCESS=true
              echo "✅ Application is responding!"
            else
              echo "Application not responding yet. Retrying ($((RETRY_COUNT+1))/$MAX_RETRIES)..."
              sleep 30
              RETRY_COUNT=$((RETRY_COUNT+1))
            fi
          done
          
          if [ "$HTTP_SUCCESS" = false ]; then
            echo "❌ Application is not responding after all retries"
            exit 1
          fi

      - name: Run smoke tests
        id: smoke-tests
        run: |
          cd source/tests
          npm install
          TEST_URL=http://${{ steps.launch-ec2.outputs.public_ip }} npm run smoke-tests
          
      # Login to ECR and push image only if smoke tests pass
      - name: Login to Amazon ECR
        if: success()
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1
      
      - name: Push image to ECR if tests pass
        if: success()
        run: |
          ECR_REGISTRY=${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG=${{ steps.build-image.outputs.image_tag }}
          
          # Tag the image for ECR
          docker tag fsv-app:$IMAGE_TAG $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG
          docker tag fsv-app:$IMAGE_TAG $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:latest
          
          # Push images to ECR
          docker push $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG
          docker push $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:latest
          
          echo "✅ Images pushed to ECR: $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG and :latest"
          
      - name: Set up SSH for QA deployment
        if: success()
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to QA EC2
        if: success()
        run: |
          # Get the EC2 hostname from the instance ID
          EC2_HOST=$(aws ec2 describe-instances \
            --instance-ids ${{ env.QA_EC2_INSTANCE_ID }} \
            --query 'Reservations[0].Instances[0].PublicDnsName' \
            --output text)
          
          # Add host key
          mkdir -p ~/.ssh
          ssh-keyscan $EC2_HOST >> ~/.ssh/known_hosts
          
          # Pass the AWS credentials to the deploy script
          ECR_REGISTRY=${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG=${{ steps.build-image.outputs.image_tag }}
          
          ssh ec2-user@$EC2_HOST "export AWS_ACCESS_KEY_ID='${{ secrets.AWS_ACCESS_KEY_ID }}' && \
                                export AWS_SECRET_ACCESS_KEY='${{ secrets.AWS_SECRET_ACCESS_KEY }}' && \
                                export AWS_SESSION_TOKEN='${{ secrets.AWS_SESSION_TOKEN }}' && \
                                export AWS_DEFAULT_REGION='${{ env.AWS_REGION }}' && \
                                cd /opt/deployment && \
                                ./deploy.sh $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG"
      
      - name: Terminate verification instance
        if: always()
        run: |
          aws ec2 terminate-instances --instance-ids ${{ steps.launch-ec2.outputs.instance_id }}